import java.util.Scanner;

public class Lesson8 {
  public static void main(String[] args) {
	  Car car1 = new Car();
	  car1.drive(10);
	  dist = <the allery obj> speed
	  dist = this.speed * time;
	  //this ---> the object upon which the method was invoked

  }

  public Car (int year) {
	  this.year = year;
  }
//class interface is what is exposed to other classes to do things within our classes
//visibility modifiers: public vs private vs protected
//Note: keep data private
//getter and setters are methods that allow the class to do a check
//Also sometimes calles accessors and mutators
  public class car {

	  public void setSpeed(double sp) {
		  Speed = Sp;
	  }

	  public double setSpeed() {
		  return speed;
	  }

	  double speed;
	  public final int WHEELS =4;
	  public double drive (int times) {
		  double dist;
		  dist = speed * times;
		  dist = this.speed * time;// the object upon which method is invoked
		  return dist
		  return dist;
	  }
  }
}
